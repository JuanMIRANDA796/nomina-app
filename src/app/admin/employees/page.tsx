import EmployeeManager from './EmployeeManager';

export const metadata = {
    title: 'Empleados | NóminaApp',
    description: 'Gestión de empleados',
};

export default function EmployeesPage() {
    return <EmployeeManager />;
}
